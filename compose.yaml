version: '3'

services:
  postgres:
    image: postgres:16
    environment:
      - 'POSTGRES_DB=etutor_fanf'
      - 'POSTGRES_PASSWORD=secret'
      - 'POSTGRES_USER=etutor_fanf'
    ports:
      - '5434:5432'
